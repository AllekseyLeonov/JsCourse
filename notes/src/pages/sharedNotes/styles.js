import { makeStyles } from "@material-ui/core/styles";

const styles = makeStyles(() => ({
  AddNoteButton: {
    marginTop: "20px",
  },
  NotesContainer: {
    display: "flex",
    width: "90%",
    height: "100vh",
    marginLeft: "auto",
    marginRight: "auto",
  },
}));

export default styles;
